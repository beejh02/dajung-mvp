# Shared Frontend Client

Shared TypeScript API client, auth session storage helpers, backend response
types, and formatting utilities for the kiosk and admin apps.

The frontend apps import this source directly until a workspace package is
introduced.
